export const config3D = {
  // Toggle the floating interactive 3D geometric shape in the Hero section
  enableFloatingHeroShape: true,

  // Toggle the custom cursor follower dot and glowing ring
  enableCustomCursor: true,

  // Speed factor for scrolling parallax (range: 0.05 to 0.3)
  parallaxStrength: 0.12,

  // Duration in milliseconds for viewport entry animations
  revealDuration: 850,

  // Card perspective tilt bounds on hover (in degrees)
  defaultTiltIntensity: 8,

  // Toggle flag to simplify 3D transforms on mobile/touch interfaces to maximize FPS
  optimizeForMobile: true,
};
